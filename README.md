# Face Comparation API
Check that two faces belong to the same person.

``` bash
# Build the docker image
$ docker build -t face_compare:v1 .

# Run the docker container
$ docker run -p 5000:5000 -d face_compare:v1
```
